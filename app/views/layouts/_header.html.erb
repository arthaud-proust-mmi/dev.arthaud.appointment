<nav class="navbar navbar-expand-lg navbar-light">
    <div class="container-md">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav me-auto">
                <%= link_to ENV['app_name'], home_url, class: class_names('nav-link', { active: current_page?(root_path) }) %>
                <%= link_to "Services", customer_services_path, class: class_names('nav-link', { active: current_page?(customer_services_path) }) %>
                <%= link_to "Espace Pro", pro_path, class: class_names('nav-link', { active: current_page?(pro_path) }) %>
            </div>
            <div class="navbar-nav">
                <% if user_signed_in? %>
                    <div class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle <% if current_page?(account_path) %>active<% end %>" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <%= current_user.name %>
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                            <%= link_to "Mon compte", account_path, class:"dropdown-item" %>
                            <%= link_to "Mes rendez-vous", my_meets_path, class:"dropdown-item" %>
                            <%= button_to "Se déconnecter", destroy_user_session_path, method: :delete, class:'dropdown-item border-0 '  %>
                        </div>
                    </div>
                <% else %>
                    <%= link_to 'Se connecter', new_user_session_path, class:"nav-link"  %>
                    <%= link_to 'Créer un compte', new_user_registration_path, class:"nav-link" %>
                <% end %>
            </div>
        </div>
    </div>
</nav>